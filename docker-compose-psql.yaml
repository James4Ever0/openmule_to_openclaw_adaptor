version: '3.1'
services:
 db:
   image: postgres:15
   restart: always
   environment:
     POSTGRES_USER: username
     POSTGRES_PASSWORD: password
    #  POSTGRES_DB: openmule_db
   ports:
     - "5432:5432"
   volumes:
     - pgdata:/var/lib/postgresql/data
volumes:
 pgdata: